{
  "name": "pomodoro-timer",
  "version": "2.0.0",
  "description": "Enhanced Pomodoro Timer with ES6 modules",
  "type": "module",
  "scripts": {
    "start": "npx http-server -p 3000 -c-1",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "vitest": "^1.0.0",
    "jsdom": "^23.0.0"
  }
}
